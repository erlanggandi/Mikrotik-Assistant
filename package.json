{
  "name": "mikrotik-assistant",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "AI MikroTik Assistant - MVP per PRD v0.2 (read-only control plane)",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=22.13"
  },
  "dependencies": {
    "express": "^4.21.2"
  }
}